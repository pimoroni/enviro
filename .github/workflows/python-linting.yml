name: Python Linting

on:
  push:
  pull_request:

jobs:
  lint:
    name: Python Linting
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v7

    - name: Install Python
      uses: actions/setup-python@v7
      with:
        python-version: '3.13'

    - name: Install Python Deps
      run: source ci/python.sh && qa_prepare_all

    - name: Lint MicroPython Firmware
      shell: bash
      run: source ci/python.sh && qa_firmware_check
